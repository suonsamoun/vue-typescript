import { createStore } from "vuex";
import { todo } from './modules';

export const store = createStore({
    state: {},
    mutations: {},
    actions: {},
    getters: {},
    modules: {
        todo
    },
});
